# 问题与待办记录

记录日期：2026-09-10。

以下保留首次检查证据，并记录后续实现状态；“新增分类偶尔缺失”的实际触发原因仍待复现。代码修改不等于已经推送或部署。

## 1. 最近更新只按日期排序

状态：已实现。完整 Git 提交时间用于排序；无 Git 记录时使用完整文件修改时间，已补充同日新增/修改与时区回归测试。以下为原问题证据：

- “最近更新”由每次构建读取真实笔记后生成，不是写死的示例列表。
- `scripts/build.mjs` 中 `gitDates()` 使用 Git 的 `%cs`，仅保留年月日；无 Git 记录时也把修改时间截取到日期。
- `loadNotes()` 按日期倒序，同一天按文件路径名称排序；首页再取前三篇。
- 后果：同日新增或修改的笔记不一定排到最前，甚至可能进不了首页前三篇；全部笔记列表也受同样排序规则影响。
- 已观察：2026-09-10 新增的“使用GitHub Pages搭建个人主页”已成功上线，但在“最近更新”中排第二，而不是按提交先后排第一。
- 后续方向：保留完整提交时间或提交先后信息用于排序，日期显示仍可保持年月日。补充同日不同时间新增、修改文章的回归验证。

## 2. 新增分类偶尔缺失、刷新后恢复

状态：用户报告的现场暂未复现；已发现页面与索引版本可能不一致的隐患，尚不能确认是这次现象的实际原因。

用户描述：新增 `notes/开发工具/GitHub/使用GitHub Pages搭建个人主页.md` 后，偶尔看不到“开发工具”一级分类，刷新后恢复。

检查证据：

- 当前线上首页、笔记列表页、六篇文章页与搜索索引都包含“开发工具”。六篇文章页均包含完整的五个一级分类，没有发现当前发布版本漏生成分类。
- 最新自定义发布工作流成功，提交为 `3c734e6085c5af210bcb6970cef579bc6ba28d2f`。此前的重复发布问题在最新几次发布中未再出现。
- 首页知识目录、笔记侧栏和文章侧栏在构建时写入各自的 HTML。
- `assets/notes.js` 单独请求 `assets/notes-index.json`，以 `cache: "no-cache"` 获取并重新验证索引，仅使用其中的 `notes` 更新文章列表。
- 分类链接在脚本开始时从 HTML 收集，没有依据新索引中的 `categories` 重建侧栏。
- 此次读取到的线上 HTML 与索引响应含 `Cache-Control: max-age=600`。这允许缓存保留十分钟的新鲜期，并不等于每次发布都一定延迟十分钟。

可能的触发过程：

1. 浏览器使用新增分类之前的旧 HTML，分类栏仍然只有四类。
2. 笔记页加载到新版索引，文章列表已包含新文章。
3. 侧栏没有随索引更新，因此新文章与旧分类栏并存；重新加载新版 HTML 后恢复。

以上是由代码结构支持的可能场景，不是对用户当时浏览器状态的复现。未获取异常发生时的页面、网络记录及导航过程，不能排除缓存、部署切换时刻或历史页面恢复等因素。

没有发现筛选逻辑会单独隐藏或删除“开发工具”一级分类。分类折叠只隐藏子项；手机布局会折叠整个分类面板，不应单独隐藏一个大类标题。

后续方向：统一分类、计数和文章列表的数据版本，评估构建版本标识与索引一致性检查。也要覆盖首页和文章页的旧版本缓存，而不是只改笔记列表页。若再次出现，记录具体 URL、是否刚发布、是否通过浏览器返回进入，以及当时的网络响应。

## 3. 一级与二级分类支持自定义展示顺序

状态：已实现。`site.config.json` 的 `categoryOrder` 和 `subcategoryOrder` 统一控制首页、笔记侧栏、文章侧栏及索引顺序。以下为原检查记录：

当前行为：

- 一级、二级分类均由 `Intl.Collator("zh-CN", { numeric: true })` 对分类名称排序。
- 与目录创建时间、文章更新时间和文章数量无关；名称中的数字按数值参与比较。
- 首页“我的知识目录”、笔记侧栏、文章侧栏和索引共用同一份分类结果。
- 此次线上一级分类顺序为：后端 → 开发工具 → 前端 → 数据库 → AI Agent。
- 空分类不显示；加入有效 Markdown 文章后才出现。

建议方案，待后续确认并实施：

- 使用一个独立配置文件指定一级分类顺序和各一级分类下的二级分类顺序。
- 配置仅控制展示顺序，分类归属仍由 Markdown 的实际目录决定。
- 已配置且存在文章的分类按指定顺序排列；新出现、未配置的分类追加在末尾，继续按名称排序。
- 配置中暂时没有文章的分类不强制显示。
- 首页、笔记页和文章页统一使用该顺序。
- 分类名、目录路径和现有文章网址不因设置顺序而改变。
- 用户尚未提供最终顺序；不把示例排列当成已确认的偏好。

## 4. 分类栏滚动阻断外部页面

状态：已实现。桌面分类栏使用 `overscroll-behavior: auto`，到达边缘后允许继续滚动页面；手机分类面板使用自然页面高度和 `overflow: visible`，不再建立嵌套滚动区域。

## 5. 文章时间、阅读信息、下载说明和浏览量

状态：已实现，等待用户推送后在正式域名验证真实计数。

- 文章页和笔记列表统一显示北京时间（精确到秒）、动态相对时间、近似字数和预计阅读时长。默认450字符/分钟，可在配置中调整。
- 下载链接明确改为“下载原文Markdown”。
- 公共页面接入不蒜子：全站页面参与站点 PV，首页显示站点累计数字，文章页显示自身 PV。不增加五分钟冷却，不轮询计数接口。
- 仅在配置的正式 HTTPS 域名加载统计脚本，本地预览不发送统计请求；等待数据或失败时不展示假数字。
- 未来栏目共用公共页面生成入口即可接入，不把统计限制在 `notes/` 路径。
- 本地自动化验证涵盖时间边界、字数、配置、页面生成、正式域名限制、重复初始化与失败状态，不向真实计数服务提交模拟访问。

## 首次检查范围（历史记录）

本次进行了源代码阅读、只读的笔记分类计算，以及线上页面、索引、响应缓存头和发布记录检查。没有运行构建、重部署或修改应用代码，也没有在 GitHub 创建 Issue；这份文件是本地仓库内的问题记录。
